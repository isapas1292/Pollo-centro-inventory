namespace PolloCentro.Api.Application.Auth;

/// <summary>Constantes de la cookie de autenticación (JWT en cookie HttpOnly).</summary>
public static class AuthCookie
{
    public const string Name = "pc_token";
}
