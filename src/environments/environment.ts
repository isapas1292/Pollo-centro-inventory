/**
 * Configuración de DESARROLLO (por defecto con `ng serve`).
 * En el build de producción se reemplaza por environment.prod.ts (ver angular.json).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
